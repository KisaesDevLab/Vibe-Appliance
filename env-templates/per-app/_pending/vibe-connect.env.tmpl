# env-templates/per-app/_pending/vibe-connect.env.tmpl — staged.
# enable-app.sh looks for env-templates/per-app/<slug>.env.tmpl at the
# top level. Move this file up once Connect is unblocked.

ALLOWED_ORIGIN=@ALLOWED_ORIGIN@
DATABASE_URL=@DATABASE_URL@
REDIS_URL=@REDIS_URL@

MIGRATIONS_AUTO=true

# --- Operator-supplied (set after enable; see admin Apps panel) ---
# PLAID_CLIENT_ID=
# PLAID_SECRET=
