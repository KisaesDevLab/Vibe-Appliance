# vibe-mybooks.env.tmpl — rendered to /opt/vibe/env/vibe-mybooks.env
# by lib/enable-app.sh. Markers: see env-templates/per-app/vibe-tb.env.tmpl.

ALLOWED_ORIGIN=@ALLOWED_ORIGIN@
DATABASE_URL=@DATABASE_URL@
REDIS_URL=@REDIS_URL@

# Appliance always runs migrations explicitly. PLAN.md §8 item 5.
MIGRATIONS_AUTO=false

# --- Optional knobs ---
# Override via 'sudo nano /opt/vibe/env/vibe-mybooks.env'.
# ANTHROPIC_API_KEY=
